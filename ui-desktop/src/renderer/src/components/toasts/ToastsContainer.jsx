import styled from 'styled-components';

const ToastsContainer = styled.div`
  position: fixed;
  top: 10px;
  left: 50%;
  width: 100%;
  max-width: 320px;
  margin-left: -160px;
  z-index: 11;
`;

export default ToastsContainer;
